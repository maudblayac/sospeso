
<header class="">
<!-- header -->
<nav class="FlexRow AlignCenter FlexBetween ">
<article>
    <img class="" alt="filter" src="{{ asset('/images/navbar/filtre.svg') }}">
    <img class="" alt="search" src="{{ asset('/images/navbar/search.svg') }}">
</article>

<article>
    <img class="" alt="logo" src="{{ asset('/images/navbar/logo.svg') }}">
</article>

<article>
    {% if is_granted("ROLE_ADMIN") %}
        <li>
            <a href="{{ path('app_admin') }}" class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-700">
            <i class="fa-solid fa-gear mr-1"></i>Administration                                </a>
        </li>
    {% endif %}
       {% if is_granted("ROLE_RESTAURANT") %}
        <li>
            <a href="{{ path('app_dashboard_restaurant') }}" class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-700">
            <i class="fa-solid fa-gear mr-1"></i>Dashboard                                </a>
        </li>
    {% endif %}
    <img class="" alt="profile" src="{{ asset('/images/navbar/profile.svg') }}">
    <img class="" alt="cart" src="{{ asset('/images/navbar/cart.svg') }}">
</article>

</nav>

</header>